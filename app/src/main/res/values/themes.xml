<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Chatzy" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!--<item name="fontFamily"></item>-->
    </style>

    <style name="Theme.Chatzy" parent="Base.Theme.Chatzy" />

    <style name="MySplash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/app_icon_without_an_icon_background</item>
        <item name="windowSplashScreenBackground">@color/grey</item>
        <item name="windowSplashScreenAnimationDuration">2000</item>
        <item name="postSplashScreenTheme">@style/Theme.Chatzy</item>
    </style>
</resources>